export class Machine {
    x: number;
    y: number;
    r: number;
    url: string;
    constructor(x: number, y:number, r:number, url:string) {
        this.x = x;
        this.y = y;
        this.r = r;
        this.url = url;
    }
}